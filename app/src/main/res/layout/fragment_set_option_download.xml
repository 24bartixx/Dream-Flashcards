<?xml version="1.0" encoding="utf-8"?>
<layout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>
        <variable
            name="appViewModel"
            type="com.example.dreamflashcards.viewmodels.AppViewModel"/>
        <variable
            name="setOptionDownloadFragment"
            type="com.example.dreamflashcards.fragments.SetOptionDownloadFragment"/>
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:padding="16dp"
        tools:context=".fragments.SetOptionDownloadFragment">

        <ImageView
            android:id="@+id/set_image"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:srcCompat="@drawable/english_icon"
            app:layout_constraintBottom_toTopOf="@id/set_name"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"/>

        <TextView
            android:id="@+id/set_name"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center"
            android:layout_marginBottom="36dp"
            android:textSize="36sp"
            android:text="@{appViewModel.currentDownloadSet.name}"
            style = "@style/HeadlineText"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintBottom_toTopOf="@id/browse_button"
            tools:text="Top Gun"/>

        <com.google.android.material.button.MaterialButton
            android:id="@+id/browse_button"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="56dp"
            android:layout_marginHorizontal="16dp"
            android:text="@string/browse_button_text"
            android:onClick="@{() -> setOptionDownloadFragment.goToTheNextScreen()}"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            style="@style/AppButton"/>

        <com.google.android.material.button.MaterialButton
            android:id="@+id/download_button"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            android:layout_marginHorizontal="16dp"
            android:text="@string/download_button_text"
            android:onClick="@{() -> setOptionDownloadFragment.download()}"
            app:layout_constraintTop_toBottomOf="@id/browse_button"
            app:layout_constraintStart_toStartOf="parent"
            style="@style/AppButton"/>

    </androidx.constraintlayout.widget.ConstraintLayout>

</layout>